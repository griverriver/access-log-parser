class PointConstr {
    int x;
    int y;

    PointConstr(int x, int y) {
        this.x = x;
        this.y = y;
    }

    public String toString() {
        return "{" + x + ";" + y + '}';
    }
}
